[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week2_warmup_nxyz109"
version = "0.0.1"
dependencies = []

[project.scripts]
contacts = "resolution_week2_warmup_nxyz109.main:main"